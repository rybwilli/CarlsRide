version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npx @angular/cli build --configuration production
  artifacts:
    baseDirectory: dist/carls-ride
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
